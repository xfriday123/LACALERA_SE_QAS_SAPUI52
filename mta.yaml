_schema-version: "3.2"
ID: semnew
version: 1.0.2
modules:
- name: semnew-approuter
  type: approuter.nodejs
  path: semnew-approuter
  requires:
  - name: semnew_html_repo_runtime
  - name: uaa_semnew
  - name: semnew-destination-service
    group: destinations
    properties:
      forwardAuthToken: true
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    memory: 256M
- name: semnew_ui_deployer
  type: com.sap.application.content
  path: .
  requires:
  - name: semnew_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nsnewuisemnew.zip
      name: nsnewuisemnew
      target-path: resources/
- name: nsnewuisemnew
  type: html5
  path: uisemnew
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: semnew_html_repo_runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
- name: uaa_semnew
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: semnew-xsuaa-service
    service-plan: application
- name: semnew_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: semnew-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: semnew-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
